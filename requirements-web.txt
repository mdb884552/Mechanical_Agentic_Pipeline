# Production dependencies for Render deployment
# gmsh excluded — not needed by dashboard (only export_stl.py uses it)

scikit-fem>=12.0
numpy>=1.24
scipy>=1.11
matplotlib>=3.7
chromadb>=0.5
anthropic>=0.30
langgraph>=1.2
python-dotenv>=1.0
requests>=2.31
fastapi>=0.100
uvicorn[standard]>=0.23
